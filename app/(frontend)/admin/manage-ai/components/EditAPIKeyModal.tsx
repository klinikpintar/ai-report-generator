interface Props {
  isVisible: boolean;
  onClose: () => void;
  providerId: string;
  providerName: string;
}

export const EditAPIKeyModal = ({ isVisible, onClose, providerId, providerName }: Props) => {
  return <></>;
};
