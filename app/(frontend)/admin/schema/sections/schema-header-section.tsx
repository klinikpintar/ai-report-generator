import React from "react";
import { HeaderSection } from "@/app/(frontend)/components/header-section";

export const SchemaHeaderSection = () => {
  return (
    <HeaderSection
      title="Manajemen Skema Database"
      subtitle="Kelola dan Atur Skema Database dengan Fleksibel"
      description="Mengelompokkan skema ke dalam service yang sesuai, memastikan validasi skema, serta melakukan perubahan jika diperlukan untuk memastikan integritas data."
      align="left"
    />
  );
};
