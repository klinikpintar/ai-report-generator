"use client";
import React from "react";
import { User } from "../types/user";

interface UserTableProps {
  users?: User[];
}

export const UserTable = ({
  users,
}: UserTableProps) => {
  return <></>
};
