"use client";
import React, { useState } from "react";

type CodeBlockProps = {
  language: string;
  value: string;
};

export const CodeBlock: React.FC<CodeBlockProps> = ({ language, value }) => {
  return <></>;
};
