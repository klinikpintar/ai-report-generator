import React from "react";
import { ManagementSchemaHeader } from "./elements";

const SchemaSection = () => {
  return (
    <section className="container mx-auto max-w-screen-lg">
      <ManagementSchemaHeader />
    </section>
  );
};

export default SchemaSection;
