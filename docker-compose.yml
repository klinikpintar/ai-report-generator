services:
  db_postgre:
    build:
      dockerfile: Dockerfile.local
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-ai-report}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-admin}
    ports:
      - ${POSTGRES_PORT:-5432}:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network
  
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - .env
  #   environment:
  #     - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db_postgre:5432/${POSTGRES_DB}
  #     - NEXT_PUBLIC_BASE_URL=http://localhost:3000
  #     - NODE_ENV=production
  #   depends_on:
  #     db_postgre:
  #       condition: service_healthy
  #   networks:
  #     - app_network

volumes:
  postgres_data:
    driver: local

networks:
  app_network:
    driver: bridge
